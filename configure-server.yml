---

- name: "configuration of server play"
  hosts: all
  become: true
  become_user: root
  become_method: sudo
  gather_facts: false


  pre_tasks:
    - nmae: "install git"
      apt:
        name: git
        state: present
   
  roles:
    - configure-server
    - setup-git
    - mysql